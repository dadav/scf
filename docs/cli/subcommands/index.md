# Subcommands

```{toctree}
---
hidden:
---

cve
config
cache
server
```

```{command-output} scf -h
```

- {doc}`cve <./cve>`
- {doc}`config <./config>`
- {doc}`cache <./cache>`
- {doc}`server <./server>`
